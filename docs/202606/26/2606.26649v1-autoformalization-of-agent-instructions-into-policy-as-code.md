---
title: Autoformalization of Agent Instructions into Policy-as-Code
title_zh: 代理指令自动形式化为策略即代码
authors: "Adam Mondl, Matthew Maisel, John H. Brock"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.26649v1"
tags: ["query:ehr-agent"]
score: 9.0
evidence: 使用MedAgentBench基准测试医疗Agent策略执行，直接相关于医疗Agent基准测试
tldr: 智能体安全在高风险领域需形式化策略执行，但现有概率护栏无保证，手工编码难以扩展。本文提出自动形式化流水线，通过LLM生成器-评论家循环将自然语言指令转为Cedar语言策略。在MedAgentBench上，自动形式化策略覆盖远超手工编码的规范。该方法首次实现自然语言到可验证策略的自动转换，兼顾效率与保证。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.26649v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1781, \"height\": 322, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26649v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1801, \"height\": 2366, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.26649v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 864, \"height\": 370, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26649v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 866, \"height\": 277, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26649v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1429, \"height\": 1173, \"label\": \"Table\"}]"
motivation: 现有智能体安全方法缺乏形式化保证或无法扩展，需自动将自然语言政策转为可验证形式化策略。
method: 采用LLM生成器-评论家循环，将智能体提示、工具描述和自然语言文档自动转化为Cedar策略语言代码。
result: 在MedAgentBench基准上，自动形式化策略覆盖的规范比例显著高于先前手工编码符号执行方法。
conclusion: 自动形式化策略可扩展、可验证，为解决智能体安全中的策略执行难题提供了可行方案。
---

## 摘要
高风险领域中的代理安全性需要形式化的策略执行，但现有的大多数方法要么依赖概率性护栏（微调分类器、基于提示的引导），这些方法不提供形式化保证，要么依赖手工编码的符号执行，这种执行无法扩展到真实策略规范的广度。我们提出了一条自动形式化流水线，该流水线使用基于LLM的生成-批评循环，将代理提示、MCP工具描述和自然语言策略文档翻译成经过形式化验证的策略。生成的策略以Cedar策略语言编写。在MedAgentBench基准上，我们的自动形式化策略相比先前工作中的手工编码符号执行，覆盖了更多的源自然语言规范。

## Abstract
Agent safety in high-stakes domains requires formal policy enforcement, but most existing approaches either rely on probabilistic guardrails (fine-tuned classifiers, prompt-based steering) that offer no formal guarantees, or on hand-coded symbolic enforcement that does not scale to the breadth of real policy specifications. We present an autoformalization pipeline that translates agent prompts, MCP tool descriptions, and natural language policy documents into formally verified policies using an LLM-based generator-critic loop. The resulting policies are written in the Cedar Policy Language. On the MedAgentBench benchmark, our autoformalized policies cover substantially more of the source natural-language specification than the hand-coded symbolic enforcement in prior work.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：高安全场景（如电子病历）中的 AI 代理需要严格的策略执行，但现有方法存在两难：基于分类器或提示的概率性护栏无法提供形式化保证；手工编码的符号执行（如 Hong et al. 的规则）难以扩展到真实复杂策略。
- **研究动机**：LLM 代理在自主调用工具时面临提示注入等攻击，需一种可自动、可验证、可扩展的策略生成与执行方法。
- **整体含义**：提出**自动形式化（autoformalization）**流水线，将自然语言代理指令、MCP 工具定义和策略文档自动转化为形式化策略代码（Policy-as-Code），并用确定性引擎在运行时执行，兼顾安全保证与可扩展性。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：采用“**验证三明治（Verification Sandwich）**”架构，分三层：
  - **基底层（Grounding Layer）**：从 MCP 工具定义程序化生成 Cedar 策略模式（schema），确定主体-资源-动作本体。
  - **模型层（Model Layer）**：使用 LLM（Gemini 3 Pro）作为生成器，结合系统提示、工具定义、策略文档和 schema 生成候选 Cedar 策略。
  - **安全层（Safety Layer）**：包括**硬评判器（Hard Critic）**和**软评判器（Soft Critic）**。硬评判器执行语法检查、模式合规、静态分析（空泛策略、矛盾规则）；软评判器是 LLM-as-Judge（Gemini 2.5 Flash），评估语义对齐和定性评分。两者反馈给生成器，形成**生成-评判循环**，迭代直至通过质量阈值。

- **关键技术细节**：
  - 策略语言：**Cedar**（Amazon 开源的授权语言），支持模式、形式化验证、静态分析和人类可读。
  - 运行时：外部确定性策略引擎，独立于 LLM 推理上下文，对抗提示注入。
  - 自动生成 Cedarschema：从 MCP 工具的 JSON schema 提取实体、动作和输入类型。
  - 两种评判：硬评判（确定性）和软评判（LLM 评估语义一致性）。
  - 循环次数：硬评判最多重试 3 次，软评判使用两阶段流程（Judge → Verifier）。

## 3. 实验设计：数据集、基准、对比方法

- **基准与数据集**：
  - **MedAgentBench**（Jiang et al. 2025）：针对电子病历（EMR）代理的功能基准，包含 300 条原始轨迹和 50 条对抗轨迹。
  - 策略来源：Hong et al. 合成的 88 条自然语言策略规则（由 GPT-5.2 编写并经 STPA 扩充），其中 23 条由手工编码的符号护栏覆盖。
  - 本文扩展：额外从 MCP 服务器代码（FHIR 约束）转录规则，共覆盖更多策略。

- **实验条件**（三种对比）：
  - **Raw**：代理使用原始 HTTP GET/POST 工具（MedAgentBench 默认）。
  - **Baseline**：每个 HTTP 端点包装为类型化的 MCP 工具。
  - **Guardrail**：在 Baseline 上叠加 Hong et al. 的符号护栏。

- **对比方法**：
  - Hong et al. 的手工编码符号护栏（仅覆盖 23 条规则）。
  - 本文的自动形式化 Cedar 策略（通过流水线自动生成，覆盖更多规则）。

- **评估指标**：
  - **Unsafe ↓**：Hong et al. 的违规率（限其 23 条规则内）。
  - **Cedar Block Rate ↑**：本策略的阻止率（覆盖全部可形式化规则）。

- **额外分析**：分离轨迹中是否包含 POST 写操作，以准确评估策略阻止效果。

## 4. 资源与算力

- **计算资源**：
  - 论文中**未明确说明**使用的 GPU 型号、数量或训练时长。
  - 主要依赖 LLM API 调用：生成器使用 Gemini 3 Pro，评判器使用 Gemini 2.5 Flash（温度 0.3/0.1），硬评判使用 Cedar 参考工具（Rust CLI，本地运行）。
  - 由于不涉及模型训练，算力需求主要来自 API 调用次数和策略验证迭代。作者未定量报告。

## 5. 实验数量与充分性

- **基本实验**：
  - 三组条件 × 两个数据集（原始 / 对抗）= 6 个主要对比（Table 1）。
  - 子分析：按轨迹是否有 POST 操作分解（Table 2）。
  - 规则触发统计：附录 Table 3 列出每条规则的触发次数及来源桶（policy.md 或 MCP）。
  - 共涉及 300 + 50 条轨迹，以及 30+ 条 Cedar 规则。

- **充分性分析**：
  - **优点**：对比了不同条件（原始/基线/护栏），覆盖正常和对抗场景，包含子分析消除无写操作轨迹的干扰，提供了详细的规则触发明细。
  - **不足**：仅依赖单一基准（MedAgentBench），未在多个代理任务或领域上验证；未与纯 LLM 护栏（如 Llama Guard）或提示工程进行完整比较；未评估策略生成的效率（时间、API 成本）；未进行消融实验（如去掉软评判器或硬评判器的效果）。实验设计总体合理但范围有限。

## 6. 论文的主要结论与发现

- **主要结论**：
  1. 自动形式化流水线能够将自然语言策略转换为可验证的 Cedar 代码，且覆盖范围显著大于手工编码（88 条规则中可形式化部分远多于 23 条）。
  2. 在 MedAgentBench 上，Cedar 阻止率（Block Rate）始终高于 Hong et al. 的 Unsafe 率，尤其在对抗场景下阻止率达 85.7%（vs 0% 在手工护栏内）。
  3. 安全性来自策略引擎与 LLM 上下文的解耦：攻击者无法通过提示注入绕过策略。
  4. 硬评判器确保语法和静态正确性，软评判器保证语义对齐，两者结合能生成高质量策略。
  5. 对仅包含 GET（读取）的轨迹，策略无法阻止（因为无写操作），但所有有写操作的轨迹均被有效阻止（Table 2 显示部分条件达 100%）。

## 7. 优点

- **方法创新**：首次将自动形式化（autoformalization）引入代理策略生成，结合 LLM 和确定性验证，实现自然语言到可执行策略的自动化翻译。
- **安全保证**：策略引擎独立于 LLM，提供形式化合规保证，对抗提示注入和推理不确定性。
- **可审计性**：Cedar 策略人类可读，便于安全工程师审核。
- **可扩展性**：自动生成模式（schema）和策略，减少手工编码负担。
- **实验设计清晰**：通过条件对比和写操作分解，直观展示了覆盖率提升和安全性增强。
- **开源贡献**：发布 Python 策略框架和 Cedar 绑定。

## 8. 不足与局限

- **评估局限性**：
  - 仅在 MedAgentBench（EMR 领域）验证，未扩展到其他高风险领域（金融、法律等）。
  - 未与现有概率性护栏（如 Llama Guard）进行定量对比，仅与手工编码符号方法比较。
  - 基准策略文档本身源于 GPT-5.2 合成，可能偏离真实临床政策。
- **方法局限**：
  - Cedar 是**无状态**的，难以处理多轮依赖、动作顺序和持久化上下文（作者承认该限制并计划未来加入时态逻辑和记忆）。
  - 自动生成策略可能过于严格或宽松，需要精心调整“黄金区域”；软评判器的质量依赖 LLM 能力。
  - 部分策略规则（如 §5.9 嵌套编码检查）因 Cedar 集合无法索引，需通过投影平坦化，存在 fidelity 瑕疵。
- **实验偏差风险**：
  - 对抗轨迹由作者设计，可能倾向于展示本方法优势。
  - 未报告自动化流水线的失败率或迭代次数，可能隐藏成本。
  - 未进行统计显著性检验。
- **应用限制**：
  - 流水线依赖商用 LLM API（Gemini），成本和延迟可能限制部署。
  - 对于完全无写操作的轨迹，系统无法提供保护（这属于领域固有特征）。

（完）
