---
title: Reinforcement Learning for Computer-Use Agents with Autonomous Evaluation
title_zh: 基于自主评估的计算机使用智能体强化学习
authors: "Marta Sumyk, Oleksandr Kosovan"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24515v1"
tags: ["query:ehr-agent"]
score: 8.0
evidence: 使用自主视觉语言模型评估的强化学习微调用于代理任务
tldr: 计算机使用智能体通过图形界面执行高级用户目标，但强化学习面临奖励信号难以扩展的问题。本文提出利用视觉语言模型进行自主评估，将最终截图与指令对比以提供终端反馈，无需手工设计奖励。针对评估器噪声，推导了噪声校正的奖励估计器用于策略优化。在多个基准上，校正后的奖励相比零样本和原始评估器微调分别提升12.6和5.1个百分点。该方法展示了自主评估作为实用奖励信号的潜力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.24515v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 900, \"height\": 516, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24515v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1516, \"height\": 722, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.24515v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1633, \"height\": 261, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24515v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1646, \"height\": 302, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24515v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1047, \"height\": 275, \"label\": \"Table\"}]"
motivation: 强化学习在图形界面环境中缺乏可扩展的、机器可读的奖励信号，任务成功往往依赖于视觉判断且难以手工指定。
method: 使用视觉语言模型作为自主评估器，对最终截图和指令进行任务完成判断，并建模为带噪声的二元奖励，通过噪声校正估计器用于PPO算法。
result: 在macOSWorld等三个基准上，校正评估器奖励相比零样本平均提升12.6个百分点，相比原始评估器微调提升5.1个百分点。
conclusion: 通过显式建模和校正评估器噪声，自主评估可作为GUI环境中强化学习的实用奖励信号。
---

## 摘要
计算机使用智能体（CUAs）通过直接感知并操作图形用户界面来执行高层用户目标。然而，针对CUAs的强化学习仍然困难，因为开放式的桌面环境很少提供可扩展、机器可读的奖励信号：任务成功通常依赖于视觉判断，且难以通过手工设计的奖励函数或密集的人工标注来指定。我们提出了一种RL微调框架，该框架利用自主视觉-语言评估作为GUI智能体的可扩展监督信号。给定最终截图和原始指令，视觉-语言模型判断任务完成情况并提供终端反馈，在策略优化过程中无需特定任务的启发式规则或人工标注。由于自主评估器并不完美，我们将其反馈建模为含噪声的二元奖励通道，并推导出用于近端策略优化的噪声校正奖励估计器。在macOSWorld、Windows Agent Arena和OSWorld上的实验表明，校正后的评估器奖励优于零样本基线和原始评估器奖励，平均成功率比零样本性能提升12.6个百分点，比原始评估器微调提升5.1个百分点。这些结果表明，当明确建模并校正评估器噪声时，自主评估可以作为GUI环境中RL的实用奖励信号。

## Abstract
Computer-Use Agents (CUAs) execute high-level user goals by perceiving and acting directly within graphical user interfaces. However, reinforcement learning for CUAs remains difficult because open-ended desktop environments rarely provide scalable, machine-readable reward signals: task success is often visually grounded and hard to specify with handcrafted reward functions or dense manual labels.   We propose an RL fine-tuning framework that uses autonomous vision-language evaluation as a scalable supervision signal for GUI agents. Given a final screenshot and the original instruction, a Vision-Language Model judges task completion and provides terminal feedback without task-specific heuristics or manual labels during policy optimization.   Because autonomous evaluators are imperfect, we model their feedback as a noisy binary reward channel and derive a noise-corrected reward estimator for Proximal Policy Optimization. Experiments across macOSWorld, Windows Agent Arena, and OSWorld show that corrected evaluator rewards outperform both zero-shot baselines and raw evaluator rewards, improving success rates by an average of 12.6 percentage points over zero-shot performance and 5.1 points over raw evaluator fine-tuning. These results suggest that autonomous evaluation can serve as a practical reward signal for RL in GUI environments when evaluator noise is explicitly modeled and corrected.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：计算机使用智能体（CUA）通过图形用户界面执行高层用户目标，但在开放桌面环境中应用强化学习（RL）面临关键瓶颈：缺乏可扩展、机器可读的奖励信号。任务成功通常依赖于视觉判断，难以手工指定奖励函数或获取密集的人工标签。
- **研究背景**：现有CUA（如UI-TARS）在OSWorld等基准上成功率仅约60%，离实用仍有差距。RL能通过交互和探索提升鲁棒性，但桌面应用不暴露明确任务完成信号。此前依赖DOM检查或人工标注的方式不具备跨应用、跨OS的泛化能力。
- **整体含义**：提出利用自主视觉-语言评估器（VLM）作为RL的监督信号，并显式建模评估器的噪声，通过校正得到无偏奖励估计，从而在无需人工标注或任务专属启发式规则的前提下实现有效的RL微调。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将自主评估器（VLM）视为含噪声的二元奖励通道，对其假阳性（e⁻）和假阴性（e⁺）错误进行建模，推导出渐近无偏的校正奖励估计器，并集成到PPO中进行策略优化。
- **关键技术细节**：
  - **自主评估**：使用Qwen2-VL-7B模型，给定最终截图和原始指令，输出二元信号˜r ∈ {0,1}（1表示任务完成）。评估器不依赖动作历史或中间推理。
  - **噪声模型**：定义e⁺ = Pr(˜r=0 | r⋆=1)（假负），e⁻ = Pr(˜r=1 | r⋆=0)（假正）。定义可分离常数D = 1 - e⁺ - e⁻ > 0。
  - **校正奖励估计器**：由E[˜r | r⋆] = e⁻ + D·r⋆，解得校正估计ˆr = (˜r - e⁻)/D，满足E[ˆr | τ] = r⋆，为渐近无偏估计。实际中使用从留出校准集估计的ˆe⁺、ˆe⁻计算ˆr♯。
  - **策略优化**：终端奖励为γ^T ˆr，使用PPO优化。优势估计通过将校正后的终端奖励回传至整个轨迹计算。
- **算法流程**（文字说明）：
  1. 构造合成任务数据集（7,560个任务，跨42个应用/OS），随机分为训练集（微调用）和校准集（估计噪声率）。
  2. 在训练集上，CUA（基于UI-TARS）与桌面环境交互，收集轨迹。
  3. 每个任务结束后，VLM评估器基于最终截图和指令输出˜r。
  4. 使用校准集估计的ˆe⁺、ˆe⁻计算校正奖励ˆr。
  5. 用PPO更新策略，奖励为γ^T ˆr，优势估计基于校正奖励。

### 3. 实验设计：数据集、基准、对比方法

- **数据集**：
  - 自行构建的合成数据集：覆盖macOS、Windows、Linux的42个应用，每个应用60个任务，共7,560个任务。任务类型包括OS工具、文件I/O、应用导航、设置等。
  - 额外使用OmniAct、GUI-World、GUIDE三个公开数据集的自然语言任务描述（仅用任务描述，不包含原有轨迹）。
- **基准（Benchmark）**：
  - macOS：macOSWorld
  - Windows：Windows Agent Arena
  - Linux：OSWorld
- **对比方法**：
  - Zero-shot基线（原始UI-TARS模型）
  - RL + 原始评估器奖励（˜r） + 统一跨OS模型
  - RL + 校正奖励估计器（ˆr） + 统一模型
  - RL + 原始评估器奖励（˜r） + 逐OS模型
  - RL + 校正奖励估计器（ˆr） + 逐OS模型（本文最终方法）

### 4. 资源与算力

- **文中未明确说明**：论文没有提及训练所使用的GPU型号、数量、总训练时长或能耗等算力信息。仅提到使用了Qwen2-VL-7B作为评估器，但对其推理成本和PPO训练的硬件需求未作具体描述。

### 5. 实验数量与充分性

- **实验数量**：
  - 自主评估器的标定实验：在macOS、Windows、Linux三个OS上分别报告了混淆矩阵（表2），共3个。
  - 主RL微调实验：在3个基准上对比5种训练配置（零样本、原始奖励/校正奖励 × 统一/逐OS），共15个结果（表3）。
  - 消融实验：对比了逐OS vs 统一模型（已包含在上述5种配置中）。
- **充分性评价**：实验覆盖了三类主流桌面操作系统和多个代表性公开基准，对比了核心变量（奖励是否校正、模型是否统一）。但缺少对校正估计器在不同噪声水平下的敏感性分析、不同PPO超参数的影响、以及更广泛分布偏移（如新应用版本、分辨率变化）下的评估。总体较充分，但可进一步加强。

### 6. 论文的主要结论与发现

- 自主VLM评估可以作为CUA强化学习的可扩展奖励信号，前提是显式建模并校正评估器噪声。
- 使用校正后的奖励估计器（ˆr）在所有三个基准上一致优于零样本基线和原始评估器奖励（˜r）。
- 具体提升：平均成功率相比零样本提升12.6个百分点，相比原始奖励微调提升5.1个百分点。
- 逐OS微调模型比统一模型效果更好（尤其在macOSWorld上提升明显），表明OS特异性很重要。
- 原始评估器奖励在一些场景（如OSWorld）可能引入偏置，导致性能不如校正奖励。

### 7. 优点

- **方法简洁且理论基础扎实**：将评估器噪声建模为混淆矩阵并推导出渐近无偏估计器，可直接嵌入PPO，无需复杂修改。
- **可扩展性强**：不依赖任务特定启发式或人工标注，适用于多种OS和应用。
- **实验覆盖全面**：在三个不同OS基准上验证，对比多种配置，展现校正奖励的一致优势。
- **分离校准集与训练集**：防止信息泄露，保证评估器噪声估计的独立性。
- **公开代码**：提供GitHub仓库，促进复现和应用。

### 8. 不足与局限

- **噪声率非平稳性**：假设e⁺、e⁻固定，但实际中评估器误差可能随任务类型、界面复杂度、训练过程状态分布变化，导致估计偏置。
- **稀疏奖励问题**：校正奖励改善准确性但未增加奖励密度，长期任务探索依然困难。
- **校准集质量依赖**：校准数据有限时，误差估计方差大，尤其在D较小（接近不可分）时放大噪声。
- **校正奖励超出[0,1]范围**：可能影响PPO数值稳定性，虽理论上仿射变换不变性成立，但实际需要仔细归一化和调参。
- **评估器盲点**：可能忽略视觉上细微但语义正确的完成，或高估基于表面线索的完成。另外，二元标签忽略部分进展和替代方案。
- **分布偏移鲁棒性未充分验证**：仅在一个基准（OSWorld）上测试了泛化，但未涉及新应用版本、不同屏幕分辨率、辅助功能设置等实际变化。
- **算力信息缺失**：未报告训练成本，不利于工业界评估实用性。

（完）
