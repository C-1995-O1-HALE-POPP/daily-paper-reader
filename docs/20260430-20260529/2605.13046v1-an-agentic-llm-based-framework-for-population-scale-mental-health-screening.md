---
title: An Agentic LLM-Based Framework for Population-Scale Mental Health Screening
title_zh: 基于智能体LLM的群体规模心理健康筛查框架
authors: "Giuliano Lorenzoni, Paulo Alencar, Donald Cowan"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13046v1"
tags: ["query:ehr-agent"]
score: 9.0
evidence: 使用电子记录的基于Agent的大语言模型心理健康筛查框架
tldr: 心理健康筛查面临临床数据激增与AI信任挑战。本文提出基于代理的LLM框架，将各阶段封装为LangChain代理，通过策略与代理引导评价逐步锁定配置。编排器代理协调预处理、检索、选择等环节。在抑郁症检测任务中，框架收敛至余弦相似度、动态Top-k和阈值0.75的稳定配置，控制评估成本且无回归。该工作展示了代理式AI在人口级心理健康筛查中的潜力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.13046v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 803, \"height\": 742, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.13046v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 899, \"height\": 1525, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.13046v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 478, \"height\": 664, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.13046v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 938, \"height\": 849, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.13046v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1889, \"height\": 772, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.13046v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1888, \"height\": 741, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.13046v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1889, \"height\": 1573, \"label\": \"Table\"}]"
motivation: 现有AI筛查方法在处理大规模非结构化临床数据时缺乏可信、可重复且自适应的框架，难以满足医疗环境需求。
method: 构建由策略和代理引导评价的LangChain代理管线，通过冻结/回滚机制锁定已验证阶段，并由编排器代理统筹预处理、检索、选择、阈值优化及解码。
result: 在基于转录本的抑郁症检测中，框架自动收敛至余弦相似度、动态Top-k、阈值0.75的稳定配置，有效控制成本并避免性能退化。
conclusion: 该代理框架提升了心理健康筛查的自动化、可信性与适应性，为大规模临床数据集上的可靠AI应用奠定了基础。
---

## 摘要
心理健康障碍影响着全球数百万人，医疗系统正日益被来自电子病历、远程医疗平台和群体筛查项目的大量临床数据所淹没。与此同时，医疗领域新型人工智能方法的出现要求能够处理特定领域非结构化临床信息并适应患者特定需求的智能框架。本文提出了一种构建稳健的基于LLM的流水线的智能体框架，其中每个阶段都被封装为一个LangChain智能体，受显式策略和代理引导评估的约束。阶段在验证后逐步锁定，确保后续调整不会在没有明确改进的情况下覆盖配置。所提出的框架从特征级探索开始，通过代理调优和冻结/回滚机制，最终由协调器智能体（Orchestrator Agent）进行全流程编排，该智能体协调预处理、检索、选择、多样性、阈值优化和解码。一个基于转录的抑郁症检测概念验证表明，该框架收敛到稳定配置，如余弦相似度、动态Top-k和阈值0.75，同时控制评估成本并避免性能回退。这些结果凸显了智能体AI在大型临床数据集上实现群体层面心理健康筛查的潜力，解决了医疗环境中对可信度、可重复性和适应性的关键挑战。

## Abstract
Mental health disorders affect millions worldwide, and healthcare systems are increasingly overwhelmed by the volume of clinical data generated from electronic records, telemedicine platforms, and population-level screening programs. At the same time, the emergence of novel AI-based approaches in healthcare calls for intelligent frameworks capable of processing domain-specific unstructured clinical information while adapting to patient-specific needs. This paper proposes an agentic framework for building robust LLM-based pipelines, where each stage is encapsulated as a LangChain agent governed by explicit policies and proxy-guided evaluation. Stages are incrementally locked once validated, ensuring that later adaptations cannot overwrite configurations without demonstrated improvement. The proposed framework evolves from feature-level exploration, through proxy-based tuning and freeze/rollback mechanisms, to full orchestration by an Orchestrator Agent that coordinates preprocessing, retrieval, selection, diversity, threshold optimization, and decoding. A proof-of-concept in transcript-based depression detection demonstrates that the framework converges to stable configurations, such as cosine similarity, dynamic Top-k, and threshold 0.75, while controlling evaluation costs and avoiding regressions. These results highlight the potential of agentic AI to enable population-level mental health screening over large clinical datasets, addressing critical challenges in trustworthiness, reproducibility, and adaptability required in healthcare environments.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有基于机器学习和大语言模型的解决方案在处理大规模、非结构化的临床数据（如电子病历、远程医疗转录文本）时，缺乏可信、可重复、可配置的流水线，难以适应患者差异、临床场景和部署条件的变化。特别是当前基于智能体的软件框架仍处于早期阶段，缺少清晰的架构角色定义、可变性管理以及非退化保证机制。
- **研究动机**：心理健康障碍已成为全球公共卫生重点，早期检测和群体规模筛查需求急剧上升。医疗系统需可扩展、智能且自适应的AI系统来自动化处理大规模临床数据，同时保证可信度和可复现性。
- **整体含义**：本文旨在提出一个基于智能体的大语言模型框架，通过策略驱动、代理引导评估和阶段锁定机制，为群体规模的心理健康筛查提供稳健、可靠、成本可控的解决方案，并推动智能体AI在临床辅助决策中的应用。

## 2. 论文提出的方法论

- **核心思想**：将自然语言分类流水线的每个阶段（如预处理、相似度度量、检索、多样性、后过滤、阈值优化、解码等）封装为**LangChain智能体**，每个智能体受**显式策略**（冻结/跳过/回滚规则）约束，并通过**代理指标**（proxy）低成本筛选配置，仅对最有希望的候选进行昂贵的全量评估（gold evaluation）。所有智能体由**编排器智能体**（Orchestrator Agent）协调，确保已锁定的阶段不被弱配置覆盖（非退化保证）。
- **关键技术细节**：
  - **流水线阶段**（9个智能体，Step 0–8）：Step 0编排器；Step 1预处理（嵌入+截断组合）；Step 2相似度（度量+阈值+索引）；Step 3排序与RAG类型（Top-k/静态/动态/混合）；Step 4多样性与平衡（MMR阈值/类别平衡）；Step 5后过滤器（去重/元数据/低置信度）；Step 6数据扩展（伪相关反馈可选）；Step 7阈值优化；Step 8解码与全量验证。
  - **代理指标系列**（Proxy families）：包括语义–检索覆盖（平均/最大相似度、命中率、覆盖率）、统计–文本平衡（词元计数、类型–词元比、停止词比）、排序稳定性（召回vs k曲线、Kendall-τ）、置信度启发式（顶部相似度间隔、示例投票熵）和低成本LLM判断（mini-judge一致性）。
  - **锁定/回滚机制**：每个步骤的配置经过代理筛选和全量验证后，若宏F1增益≥阈值（如0.02或0.01）则锁定；否则跳过或回滚到基线。一旦锁定，后续步骤不能覆写，除非证明有显著改进。非退化政策确保宏F1和召回率不低于基线。
- **算法流程（文字说明）**：
  1. 离线构建案例库：使用SentenceTransformer将训练集转录文本嵌入并归一化存储。
  2. 在线评估：对每个查询转录，计算嵌入，通过余弦相似度（≥阈值τ并回退到Top-k）检索最相似示例，构建包含临床例子的提示，由LLM裁判（GPT-4o-mini）生成预测，解析为二分类标签，计算准确率、宏F1、精确率、召回率和混淆矩阵。
  3. 编排器智能体依次调用Step 1–8，每个智能体执行局部策略，使用代理指标筛选候选，对通过筛选的候选用低成本LLM裁判评估，若增益显著则进一步用GPT-4全量评估并锁定。最终解码阶段需通过非退化检查才能替换默认配置。

## 3. 实验设计

- **数据集**：**DAIC-WOZ**（Distress Analysis Interview Corpus – Wizard-of-Oz），一个公开可用的临床数据集，包含189个临床访谈（转录文本），来自人类或动画自动采访者（Ellie）。提供参与者ID、二进制抑郁标志（PHQ8_Binary，0=抑郁，1=非抑郁）、严重度分数（PHQ8_Score，>10为抑郁）和性别（未使用）。数据集分为训练、开发（验证）和测试子集。论文使用扩展划分（extended split）。
- **基准**：没有显式对比其他方法，而是框架自身逐步锁定基线配置（K=5, 动态选择, 无MMR, 余弦相似度, 阈值0.75, 温度0.0, top-p=1.0, n=1）。该基线在验证集上达到acc=0.857, macroF1=0.825, rec1=0.875。后续步骤与此基线比较，若低于基线则拒绝/回滚。
- **对比方法**：无外部对比，属于框架自身的消融和策略验证。对比了不同配置（如不同k、不同阈值、是否启用MMR/后过滤器/PRF/解码参数等）下的性能。

## 4. 资源与算力

- **明确说明**：所有实验在 **Intel i7 CPU + 16GB RAM** 上运行，**未使用GPU加速**。无需FAISS等重向量索引，仅用NumPy数组计算余弦相似度。
- **框架**：Python 3.12, PyTorch 2.8, HuggingFace Transformers 4.55, Sentence-Transformers 5.1, LangChain（核心+OpenAI绑定）。LLM裁判调用OpenAI的GPT-4o-mini（低成本）和GPT-4（全量评估，每次30个样本）。
- **注意**：未提供训练时长或具体GPU型号（因未使用GPU），也未报告API调用总成本。

## 5. 实验数量与充分性

- **实验数量**：进行了**逐步探索**，每个步骤探索了若干组合：
  - Step 1：6种嵌入×截断组合 → 代理筛选50样本 → 低成本LLM评估 → 冻结。
  - Step 2：24种相似度组合 → 50样本代理 → 低成本LLM → GPT-4（30样本）→ 冻结。
  - Step 3：9种Top-k与RAG类型组合 → 30样本代理 → 低成本LLM → GPT-4（30样本）→ 冻结。
  - Step 4：4种MMR与类别平衡组合 → 30样本代理 → 低成本LLM → 若增益≥0.01则GPT-4（30样本）→ 冻结。
  - Step 5：3种后过滤器切换 → 同上。
  - Step 6：可选PRF → 代理+低成本LLM → 若增益则GPT-4（≤15样本）。
  - Step 7：在[0.70,0.80]范围内扫描阈值 → 在验证集上选最佳宏F1。
  - Step 8：解码参数小网格（温度0.0/0.1/0.2，top-p 1.0/0.9，n 1/3）→ 全量验证。
- **充分性评估**：
  - **优点**：逐步探索覆盖了嵌入、截断、相似度、k、RAG类型、多样性、后过滤、扩展、阈值、解码参数等多个维度；使用代理指标降低了成本；非退化政策确保了每一步的改进都是稳健的。
  - **不足**：
    1. 仅使用**单一数据集**（DAIC-WOZ），缺乏跨数据集的泛化验证。
    2. **未与现有方法对比**（如传统ML分类器、非智能体RAG、简单LLM微调等），因此难以评判该框架相对于现有技术的优势。
    3. 验证规模较小（全量评估仅30–45个样本），可能是受成本限制，但样本量偏少可能影响统计显著性。
    4. 由于LLM的随机性，单次运行结果可能波动，文中也承认这一点，但未进行多次重复实验来估计置信区间。

## 6. 论文的主要结论与发现

- **框架收敛到稳定配置**：最终锁定配置为余弦相似度、动态Top-k（K=5）、阈值τ≈0.75、不启用MMR、无后过滤器、无PRF、温度0.0、top-p=1.0、n=1。该配置在验证集上达到acc=0.857, macroF1=0.825, rec1=0.875。
- **非退化政策有效**：后续的阈值扫描（Step 7）和解码参数扫描（Step 8）产生的备选配置均低于基线宏F1，因此被拒绝，保留了原始配置。这验证了策略驱动锁定机制能防止回归。
- **代理引导评估**：通过低成本代理指标筛选，显著减少了昂贵的全量评估调用，平衡了性能与成本。
- **可复现性和适应性**：通过将LLM调用视为可配置软件组件并使用LangChain编排，框架具有模块化和可审计性，适用于群体规模筛查。
- **潜在适用性**：该框架可扩展至电子健康记录、大规模筛查平台和远程医疗基础设施，处理数百万临床交互。

## 7. 优点

- **架构设计清晰**：将复杂流水线解耦为多个具有明确角色和策略的智能体，提高了模块化和可维护性。
- **非退化保证机制**：通过冻结/回滚策略确保了已验证配置不被弱配置覆盖，提升了系统的稳健性和可信度。
- **成本控制**：巧妙使用代理指标（如语义相似度、统计平衡、低成本LLM裁判）进行快速筛选，仅对最有希望的部分进行全量评估，大幅降低API调用成本。
- **易于复现**：不使用GPU，仅依赖CPU和开源框架（LangChain, HuggingFace等），降低了复现门槛。
- **适应性与可扩展性**：框架支持运行时注入新策略，便于随着LLM和检索技术发展而扩展。
- **面向群体规模**：虽在中小型数据集上验证，但设计目标就是处理大规模数据，并考虑了分布式部署和成本约束。

## 8. 不足与局限

- **实验验证范围有限**：
  - 仅在一个公开抑郁检测数据集（DAIC-WOZ）上测试，缺乏跨领域（如焦虑、PTSD）或跨语言验证。
  - 未与现有最先进方法进行定量比较（如微调BERT、传统机器学习分类器或其他RAG变体），说服力不足。
- **评估样本量小**：全量评估仅使用30–45个验证样本，可能无法可靠反映真实性能。尤其来自LLM的随机性，单次评估结果可能不稳定。
- **未进行多次重复实验**：论文承认随机变异性，但未报告多轮运行的平均值和标准差，因此无法评估配置差异的统计显著性。
- **依赖商业LLM API**：使用GPT-4和GPT-4o-mini，存在成本、隐私和可复现性问题（API模型可能更新导致结果不同）。在群体规模部署中成本可能变得可观。
- **未讨论失败案例和误差分析**：未详细分析误分类案例，也未探索模型在语言偏误、文化背景、对话长度等因素上的鲁棒性。
- **未考虑实时性要求**：对于大规模筛查，响应时间可能是关键指标，论文未讨论延迟问题（虽然提及CPU可接受，但未量化）。
- **缺乏临床验证**：属于技术框架论文，未与临床专家或真实筛查流程进行对比验证，实际部署价值有待进一步研究。

（完）
