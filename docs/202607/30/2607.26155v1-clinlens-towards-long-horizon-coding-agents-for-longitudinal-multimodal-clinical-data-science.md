---
title: "ClinLens: Towards Long-Horizon Coding Agents for Longitudinal Multimodal Clinical Data Science"
title_zh: ClinLens：面向纵向多模态临床数据科学的长期编码智能体
authors: "Yuan Zhu, Ethan B. Liu, Frank Nie, Jindong Han"
date: 2026-07-28
pdf: "https://arxiv.org/pdf/2607.26155v1"
tags: ["query:ehr-agent"]
score: 9.0
evidence: 基于EHR的临床决策支持智能体基准测试
tldr: "临床数据科学代理需处理异构纵向记录，但现有基准孤立评估不同任务。提出CLINLENS基准，含200个可执行任务，覆盖MIMIC结构化电子病历、文本、心电图、胸片和超声心动图，基于4×5分类法（4种患者时间范围×5种分析能力）。采用程序优先反向合成，将每个有界半原始包与评估者私有工作流配对，检查工件、队列和时间语义及答案。最强配置在126任务子集上达56.3% STRICTPASS，执行成功率100%；参考编码代理解决83/126任务，五个生物医学系统最高2.9%。结果揭示可运行提交与正确临床分析间的巨大差距。"
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.26155v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1458, \"height\": 1161, \"label\": \"Table\"}]"
motivation: 现有基准分离医疗问答、结构表推理等，缺乏对异构纵向临床数据的综合评估。
method: 提出CLINLENS基准，含200个可执行任务，基于4×5分类法，采用程序优先反向合成生成有界半原始包和私有参考工作流。
result: "最强配置在126任务子集上达56.3% STRICTPASS，执行成功率100%；参考编码代理解决83/126任务，五个生物医学系统最高2.9%。"
conclusion: 可运行提交与正确临床分析间差距显著，CLINLENS为长程临床数据科学代理提供挑战性基准。
---

## 摘要
临床数据科学智能体必须将异质的纵向记录转化为可审计的分析，然而现有基准大多孤立地处理医学问答、结构化表格推理或通用科学数据库。我们推出了CLINLENS，这是一个包含200个可执行任务的基准，涵盖五个关联的MIMIC资源，包括结构化电子健康记录、笔记、心电图、胸部X光片和超声心动图。一个4×5分类法交叉了四个患者时间范围与五种分析能力。程序优先的逆向合成为每个有界半原始包配对一个评估者私有的参考工作流，并检查所需的工件、队列和时间语义以及最终答案。在固定的126个任务套件中，24个标准化模型-支架配置中最强的达到了56.3%的范围宏STRICTPASS，尽管EXECSUCCESS为100%。作为参考，一个单独配置的编码智能体解决了126个任务中的83个，而五个适应了GPT-4o-mini的生物医学系统最多达到2.9%的范围宏STRICTPASS。这些结果揭示了可运行提交与正确临床分析之间的巨大差距。

## Abstract
Clinical data-science agents must transform heterogeneous longitudinal records into auditable analyses, yet existing benchmarks largely isolate medical question answering, structured-table reasoning, or generic scientific repositories. We introduce CLINLENS, a benchmark of 200 executable tasks over five linked MIMIC resources spanning structured electronic health records, notes, electrocardiograms, chest radiographs, and echocardiograms. A 4 x 5 taxonomy crosses four patient-time scopes with five analysis capabilities. Program-first reverse synthesis pairs each bounded semi-raw package with an evaluator-private reference workflow and checks required artifacts, cohort and temporal semantics, and the final answer. On a fixed 126-task suite, the strongest of 24 standardized model-scaffold configurations achieves 56.3% scope-macro STRICTPASS despite 100% EXECSUCCESS. For reference, a separately configured coding agent solves 83 of 126 tasks, while five biomedical systems adapted to GPT-4o-mini reach at most 2.9% scope-macro STRICTPASS. These results expose a substantial gap between runnable submissions and correct clinical analyses.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：临床数据科学智能体需要将异质的纵向记录（如电子健康记录、笔记、心电图、胸片、超声心动图）转化为可审计的分析。现有基准要么孤立地评估医学问答、结构化表格推理，要么局限于通用科学数据库，无法联合测试智能体在多源、患者时间语义约束下的可执行分析能力。具体缺失的是：智能体是否能在整个分析过程中**保留患者-时间语义**，并生成可追溯的工件。
- **研究动机**：填补“可执行分析”与“正确临床分析”之间的评估空白。现有基准（如EHRAgent、MedAgentBench）虽然评估了执行能力，但未要求最终的答案正确性和工件一致性；而传统的医学问答基准又缺乏多步分析流程。
- **整体含义**：通过引入ClinLens，将评估焦点从“能否运行代码”转向“能否产生正确的、可审计的临床分析”，揭示当前智能体在复杂临床数据科学任务上的巨大差距。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 2.1 核心思想
- **任务定义**：每个任务是一个四元组 `τ = (q, P, A, f, ε)`，其中 `q` 是自然语言请求，`P` 是有限数据包，`A` 是工件规格，`f` 是评估者私有的参考工作流，`ε` 是答案匹配谓词。智能体接收 `(q, P, A)`，必须产出所需工件和 `final_answer.json`。
- **4×5 分类法**：四个患者时间范围（全患者、入院、ICU停留、事件/检查） × 五种分析能力（概要分析、关联分析、事件对齐的变化估计、预测、表型发现），共20个模板。
- **程序优先的逆向合成**：针对每个模板，确定性参考程序定义队列、时间窗口、随机种子、工件合约、验证谓词和参考答案；然后据此实例化数据包和自然语言请求。自动化质量控制确保所有200个任务可执行且内部一致。

### 2.2 关键技术细节
- **数据源**：连接五个MIMIC资源：MIMIC-IV结构化记录、MIMIC-IV-Note（文本笔记）、MIMIC-IV-ECG（心电图）、MIMIC-CXR-JPG（胸片）、MIMIC-IV-ECHO（超声心动图）。保留源标识符层级（患者→住院→ICU停留→检查），保留重复测量和一对多关系。
- **评估标准**：任务通过的五个条件：工件存在、输出可解析、队列行数与合约匹配、验证谓词满足、最终答案正确。数字答案有容差，类别答案用标准化精确匹配。联合称为 **STRICT PASS**；另单独报告 **EXEC SUCCESS**（仅要求运行完成且提交完整可解析）。
- **固定实验套件**：从200个任务中选取126个任务（涵盖所有20个模板，含37个全患者、31个入院、22个ICU停留、36个事件/检查任务），用于受控对比。

## 3. 实验设计

### 3.1 数据集 / 场景
- 所有任务基于MIMIC系列数据集（波士顿贝斯以色列女执事医疗中心），但ClinLens提供的是**有界半原始数据包**（bounded semi-raw packages），而非预处理的宽表。数据包包含真实源表、标识符、时间戳，智能体需要自行完成连接、时间过滤、特征工程、建模和验证。

### 3.2 基准（Benchmark）
- **ClinLens本身**就是提出的基准，包含200个任务（170个数值型、30个类别型），其中126个为固定实验套件。
- **对比方法**：
  - **标准化系统**：4个基座模型（DeepSeek-V4-Pro、GLM-5.2、GPT-5.5、Claude-Opus-4.8） × 6种支架（NoDataGuess、ReAct、DataVoyager、Reflexion、SelfDebug、RAG-ReAct），共24种配置，每个配置在126任务上运行一次（共3024次任务运行）。
  - **编码智能体**：一个单独配置的Codex（未详细说明支架）在126任务上运行，解决83个。
  - **五个生物医学系统**：BioMedAgent、BixBench、BioMaster、EHRAgent、Biomni，均适配到GPT-4o-mini，在相同126任务上评估。

### 3.3 实验设置
- 零样本提示，温度0，代码超时1小时，最多3轮生成或修复。
- 所有基于数据包的智能体收到相同的包字节和清单。

## 4. 资源与算力

- **论文未明确说明**使用的GPU型号、数量或训练时长。仅提到所有实验在标准集群上运行，每个任务有1小时超时。3024次任务运行加上智能体调用，推测使用了云端API（如GPT-5.5、Claude-Opus等）或GPU服务器，但具体硬件细节缺失。

## 5. 实验数量与充分性

### 5.1 实验数量
- **标准化系统**：4个模型 × 6个支架 = 24个配置，每个在126任务上运行一次 → 3024次任务运行。
- **编码智能体**：1个配置 × 126任务 = 126次。
- **生物医学系统**：5个系统 × 126任务 = 630次。
- **总计**：约3780次任务评估，覆盖20个任务模板。

### 5.2 充分性与公平性
- **充分性**：实验规模较大，覆盖了不同模型、支架和生物医学系统，且任务模板覆盖了所有分类组合。但一次性的单次运行（one rollout per configuration）可能引入随机性，论文承认这一点，并建议未来进行多次重复实验。
- **公平性**：
  - 标准化系统使用相同的提示策略和超参数（温度0、零样本），控制变量良好。
  - 生物医学系统均适配到相同的基座模型GPT-4o-mini，但适配方式可能影响性能，论文指出这些结果表征的是适配本身而非生物医学专用的能力，缺乏匹配的通用智能体控制组（即GPT-4o-mini配上通用支架的结果）。
  - 编码智能体（Codex）使用不同的支架（交互式），与标准化系统不可直接比较。
  - 所有系统都无权访问参考工作流或答案，评估独立。
- **偏差风险**：数据仅来自一个健康系统（MIMIC），任务由同一团队合成，可能存在系统性偏差。没有人类专家基线，无法估计任务难度上限。

## 6. 论文的主要结论与发现

1. **执行不等于正确性**：最佳标准化配置（GPT-5.5 + SelfDebug）在100%执行成功率下仅达到56.3%的STRICT PASS。许多配置几乎完成所有任务但正确率不足一半。代码智能体解决83/126任务，但所有错误均是可解析但答案错误。
2. **支架排名依赖基座模型**：SelfDebug对DeepSeek-V4-Pro和GPT-5.5最好，ReAct对GLM-5.2最好，RAG-ReAct对Claude-Opus-4.8最好。RAG-ReAct对Claude提升10个百分点，对GPT-5.5反而下降近27个百分点。
3. **生物医学系统在ClinLens上表现挣扎**：最高仅2.9% STRICT PASS（BioMedAgent），EHRAgent虽有62.6%执行成功率但只有2.2%正确率。说明现有生物医学智能体无法处理多源、纵向、跨模态的临床数据分析任务。
4. **差距可量化**：ClinLens将“可运行提交”与“正确临床分析”之间的差距转化为可复现的指标，为提升可靠性的临床数据科学智能体提供了靶标。

## 7. 优点

- **综合性强**：首次联合评估五个临床数据模态（EHR、笔记、ECG、CXR、ECHO），保留了源标识符和时间关系，避免了预处理的简化。
- **评估粒度细**：不仅检查最终答案，还检查工件存在性、队列行数、验证谓词等，确保分析过程的可审计性。
- **任务设计系统化**：4×5分类法覆盖了临床研究中的典型分析能力，任务由确定性程序合成，保证可复现性。
- **受控实验框架**：标准化了模型、支架、数据包，使不同系统的比较相对公平。
- **暴露关键问题**：明确揭示了执行与正确性之间的巨大鸿沟，为未来研究提供了明确的改进方向。

## 8. 不足与局限

- **数据单一性**：所有数据来自MIMIC（单一健康系统），限制了泛化性。未来需扩展到更多机构和数据源。
- **缺乏人类基线**：未报告人类专家（如临床数据科学家）在同样任务上的表现，无法判断56.3%相对人类水平如何。
- **单次运行**：每个配置只运行一次（one rollout），未统计方差。任务结果可能受随机性影响。
- **任务合成偏差**：任务由相同的参考程序自动生成，可能遗漏真实世界中的模糊性、缺失值模式或异常处理要求。
- **生物医学系统评估不完整**：仅测试了适配到GPT-4o-mini的版本，未测试使用更强基座模型（如GPT-5.5）或原生多模态系统的版本，结论不能推广到所有生物医学智能体。
- **安全与隐私**：论文未讨论在真实临床环境下部署此类智能体的安全性、公平性或隐私风险。

（完）
